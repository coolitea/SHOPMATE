import * as React from 'react';
import { MainTemplate } from 'components';
import { HomeContainer } from 'containers'
const Home: React.FC = () => (
  <MainTemplate>
    <HomeContainer />
  </MainTemplate>
);

export default Home;